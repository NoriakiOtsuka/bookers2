<div>
	<%= form_for(@book) do |f| %>
	<!--Alert book版消した-->
	
		<div>
			<h4>Title</h4>
			<%= f.text_field :title %>
		</div>
		<div>
			<h4>Opinion</h4>
			<%= f.text_area :body %>
		</div>
		<div><%= f.submit "Create Book" %></div>
	<% end %>
</div>
